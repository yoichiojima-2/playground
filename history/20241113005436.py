# The below code generates a list of even numbers from 1 to 20

# Using list comprehension to create a list of even numbers
even_numbers = [num for num in range(1, 21) if num % 2 == 0]

# Outputting the list of even numbers
print(even_numbers)
