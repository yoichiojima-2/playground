# Initialize a variable with a list of integers
numbers = [1, 2, 3, 4, 5]

# Loop through the list and print the square of each number
for num in numbers:
    print(num ** 2)

# Assign the result to a variable called 'result'
result = numbers

# Output the final result
print(result)
