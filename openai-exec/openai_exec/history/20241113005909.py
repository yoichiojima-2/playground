# This code will generate a list of odd numbers from 1 to 10 using list comprehension

# Using list comprehension to generate a list of odd numbers
odd_numbers = [num for num in range(1, 11) if num % 2 != 0]

print(odd_numbers)
