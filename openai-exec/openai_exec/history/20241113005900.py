# This code generates a list of even numbers from 1 to 20

# List comprehension to generate even numbers
even_numbers = [num for num in range(1, 21) if num % 2 == 0]

# Print the list of even numbers
print(even_numbers)
